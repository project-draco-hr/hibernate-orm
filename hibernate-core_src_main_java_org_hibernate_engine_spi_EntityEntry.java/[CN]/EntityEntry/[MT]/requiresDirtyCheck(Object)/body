{
  return isModifiableEntity() && (getPersister().hasMutableProperties() || !getPersister().getInstrumentationMetadata().isInstrumented() || getPersister().getInstrumentationMetadata().extractInterceptor(entity).isDirty());
}
