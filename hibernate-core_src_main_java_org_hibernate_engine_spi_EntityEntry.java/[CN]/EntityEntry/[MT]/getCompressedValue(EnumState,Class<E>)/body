{
  @SuppressWarnings("unchecked") E[] enumConstants=(E[])state.getEnumConstants();
  int index=((compressedState & state.getMask()) >> state.getOffset()) - 1;
  return index == -1 ? null : enumConstants[index];
}
