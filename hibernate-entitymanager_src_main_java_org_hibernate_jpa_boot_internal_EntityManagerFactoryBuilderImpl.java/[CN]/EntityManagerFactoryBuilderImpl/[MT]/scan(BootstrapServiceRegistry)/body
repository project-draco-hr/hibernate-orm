{
  final Scanner scanner=locateOrBuildScanner(bootstrapServiceRegistry);
  final ScanOptions scanOptions=determineScanOptions();
  return scanner.scan(new StandardJpaScanEnvironmentImpl(persistenceUnit),scanOptions,new ScanParameters(){
    @Override public JandexInitializer getJandexInitializer(){
      return null;
    }
  }
);
}
