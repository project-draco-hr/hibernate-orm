{
  this.jdbcEnvironment=jdbcEnvironment;
  this.extractionContext=new ImprovedExtractionContextImpl(serviceRegistry,jdbcEnvironment,connectionContext,defaultNamespace.getCatalog(),defaultNamespace.getSchema(),this);
  this.extractor=new InformationExtractorJdbcDatabaseMetaDataImpl(extractionContext);
  initializeSequences();
}
