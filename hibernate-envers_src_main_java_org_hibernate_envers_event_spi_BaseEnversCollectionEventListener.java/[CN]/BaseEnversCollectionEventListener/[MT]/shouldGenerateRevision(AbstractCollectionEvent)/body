{
  final String entityName=event.getAffectedOwnerEntityName();
  return getAuditConfiguration().getGlobalCfg().isGenerateRevisionsForCollections() && getAuditConfiguration().getEntCfg().isVersioned(entityName);
}
