{
  if (ClassDescriptorImpl.class.isInstance(classDescriptor)) {
    classDescriptorSet.add(classDescriptor);
  }
 else {
    classDescriptorSet.add(new ClassDescriptorImpl(classDescriptor.getName(),classDescriptor.getStreamAccess()));
  }
}
