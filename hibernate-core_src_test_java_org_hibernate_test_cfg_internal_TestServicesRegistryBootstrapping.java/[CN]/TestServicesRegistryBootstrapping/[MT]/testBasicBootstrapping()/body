{
  ServiceRegistryImpl servicesRegistry=new ServicesRegistryBootstrap().initiateServicesRegistry(ConnectionProviderBuilder.getConnectionProviderProperties());
  assertNotNull(servicesRegistry.getService(JdbcServices.class));
}
