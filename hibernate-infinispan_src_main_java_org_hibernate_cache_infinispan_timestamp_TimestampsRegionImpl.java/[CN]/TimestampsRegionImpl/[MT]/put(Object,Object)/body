{
  Transaction tx=suspend();
  try {
    cacheAdapter.withFlags(FlagAdapter.FORCE_ASYNCHRONOUS,FlagAdapter.SKIP_CACHE_LOAD).put(key,value);
  }
 catch (  Exception e) {
    throw new CacheException(e);
  }
 finally {
    resume(tx);
  }
}
