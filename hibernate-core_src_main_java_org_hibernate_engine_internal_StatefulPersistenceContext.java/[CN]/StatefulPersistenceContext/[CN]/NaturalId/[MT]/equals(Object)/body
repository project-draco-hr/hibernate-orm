{
  if (this == other) {
    return true;
  }
  if (other == null || getClass() != other.getClass()) {
    return false;
  }
  final NaturalId that=(NaturalId)other;
  return persister.equals(that.persister) && Arrays.equals(values,that.values);
}
