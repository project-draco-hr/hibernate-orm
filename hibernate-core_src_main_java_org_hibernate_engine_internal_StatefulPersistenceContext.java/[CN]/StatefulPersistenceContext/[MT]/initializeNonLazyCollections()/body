{
  if (loadCounter == 0) {
    if (tracing)     LOG.trace("Initializing non-lazy collections");
    loadCounter++;
    try {
      int size;
      while ((size=nonlazyCollections.size()) > 0) {
        nonlazyCollections.remove(size - 1).forceInitialization();
      }
    }
  finally {
      loadCounter--;
      clearNullProperties();
    }
  }
}
