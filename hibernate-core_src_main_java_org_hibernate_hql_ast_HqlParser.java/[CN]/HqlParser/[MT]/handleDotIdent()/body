{
  if (LA(1) == DOT && LA(2) != IDENT) {
    HqlToken t=(HqlToken)LT(2);
    if (t.isPossibleID()) {
      LT(2).setType(IDENT);
      LOG.debugf("handleDotIdent() : new LT(2) token - %s",LT(1));
    }
  }
}
