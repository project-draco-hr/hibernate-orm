{
  super(queryBuilder,javaType,lhs,joinProperty,(ManagedType<X>)queryBuilder.getEntityManagerFactory().getMetamodel().type(javaType));
  this.managedType=(ManagedType<X>)getModel();
  this.joinType=joinType;
}
