{
  super(criteriaBuilder,javaType,lhs,joinProperty,(ManagedType<X>)criteriaBuilder.getEntityManagerFactory().getMetamodel().managedType(javaType));
  this.managedType=(ManagedType<X>)getModel();
  this.joinType=joinType;
}
