{
  JoinImpl<Z,X> correlation=new JoinImpl<Z,X>(queryBuilder(),getJavaType(),(PathImpl<Z>)getParentPath(),getAttribute(),getJoinType());
  correlation.defineJoinScope(subquery.getJoinScope());
  correlation.correlationParent=this;
  return correlation;
}
