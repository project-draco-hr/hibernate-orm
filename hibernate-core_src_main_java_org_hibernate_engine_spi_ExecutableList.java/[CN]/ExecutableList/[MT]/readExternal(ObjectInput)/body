{
  sorted=false;
  querySpaces=null;
  int size=in.readInt();
  executables.ensureCapacity(size);
  if (size > 0) {
    for (int i=0; i < size; i++) {
      E e=(E)in.readObject();
      executables.add(e);
    }
  }
}
