{
  Printer print=new Printer(factory);
  if (positionalParameterValues.length != 0)   LOG.parameters(print.toString(positionalParameterTypes,positionalParameterValues));
  if (namedParameters != null)   LOG.namedParameters(print.toString(namedParameters));
}
