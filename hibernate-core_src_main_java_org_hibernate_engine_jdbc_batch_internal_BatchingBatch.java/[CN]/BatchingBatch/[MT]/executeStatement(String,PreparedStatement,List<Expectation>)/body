{
  try {
    checkRowCounts(sql,ps.executeBatch(),ps,expectations);
  }
 catch (  SQLException e) {
    LOG.sqlExceptionEscapedProxy(e);
    throw getSqlExceptionHelper().convert(e,"could not execute statement: " + sql);
  }
}
