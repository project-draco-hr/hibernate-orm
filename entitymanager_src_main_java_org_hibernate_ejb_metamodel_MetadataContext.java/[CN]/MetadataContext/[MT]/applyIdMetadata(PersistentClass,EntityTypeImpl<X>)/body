{
  if (persistentClass.hasIdentifierProperty()) {
    jpaEntityType.getBuilder().applyIdAttribute(attributeFactory.buildIdAttribute(jpaEntityType,persistentClass.getIdentifierProperty()));
  }
 else {
    jpaEntityType.getBuilder().applyIdClassAttributes(buildIdClassAttributes(jpaEntityType,persistentClass));
  }
}
