{
  try {
    ResultSet rs=jdbcCoordinator.getLogicalConnection().getJdbcServices().getDialect().getResultSet(statement);
    postExtract(rs,statement);
    return rs;
  }
 catch (  SQLException e) {
    throw sqlExceptionHelper().convert(e,"could not extract ResultSet");
  }
}
