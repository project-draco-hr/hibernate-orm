{
  if (isJdbc4) {
    try {
      return statement.isWrapperFor(type);
    }
 catch (    SQLException e) {
    }
catch (    Throwable e) {
      isJdbc4=false;
    }
  }
  return type.isInstance(statement);
}
