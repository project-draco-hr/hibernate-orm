{
  if (!methodLookedUp) {
    try {
      String readMethodName="get" + name.substring(0,1).toUpperCase(Locale.ENGLISH) + name.substring(1);
      method=clazz.getMethod(readMethodName);
    }
 catch (    Exception ex) {
    }
    methodLookedUp=true;
  }
  return method;
}
