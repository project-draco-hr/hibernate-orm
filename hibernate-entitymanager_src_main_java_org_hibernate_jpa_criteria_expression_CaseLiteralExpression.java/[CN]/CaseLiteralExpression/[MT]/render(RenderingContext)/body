{
  if (getJavaType() == Boolean.class && Boolean.class.isInstance(getLiteral())) {
    return super.render(renderingContext);
  }
  return CastFunction.CAST_NAME + '(' + super.render(renderingContext)+ " as "+ renderingContext.getCastType(getJavaType())+ ')';
}
