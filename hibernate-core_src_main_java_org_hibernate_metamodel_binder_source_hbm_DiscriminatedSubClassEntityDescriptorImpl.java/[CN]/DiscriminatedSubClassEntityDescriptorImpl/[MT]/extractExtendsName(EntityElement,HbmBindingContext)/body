{
  final String extendsName=((XMLSubclassElement)entityClazz).getExtends();
  if (StringHelper.isEmpty(extendsName)) {
    throw new MappingException("Subclass entity mapping [" + bindingContext.determineEntityName(entityClazz) + "] was not contained in super entity mapping",bindingContext.getOrigin());
  }
  return extendsName;
}
