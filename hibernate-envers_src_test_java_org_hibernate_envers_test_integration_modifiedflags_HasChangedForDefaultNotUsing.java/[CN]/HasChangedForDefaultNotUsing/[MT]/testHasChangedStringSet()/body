{
  List list=queryForPropertyHasChanged(NotUsingModifiedFlagsEntity.class,entityId,"stringSet");
  assertEquals(3,list.size());
  assertEquals(TestTools.makeList(1,7,8),extractRevisionNumbers(list));
}
