{
  List list=queryForPropertyHasChanged(NotUsingModifiedFlagsEntity.class,entityId,"entitiesMap");
  assertEquals(2,list.size());
  assertEquals(TestTools.makeList(1,11),extractRevisionNumbers(list));
}
