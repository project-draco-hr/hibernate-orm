{
  List list=queryForPropertyHasChanged(NotUsingModifiedFlagsEntity.class,entityId,"referencing");
  assertEquals(2,list.size());
  assertEquals(TestTools.makeList(5,6),extractRevisionNumbers(list));
}
