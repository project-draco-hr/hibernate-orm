{
  List list=queryForPropertyHasChanged(NotUsingModifiedFlagsEntity.class,entityId,"entitiesSet");
  assertEquals(3,list.size());
  assertEquals(TestTools.makeList(1,10,11),extractRevisionNumbers(list));
}
