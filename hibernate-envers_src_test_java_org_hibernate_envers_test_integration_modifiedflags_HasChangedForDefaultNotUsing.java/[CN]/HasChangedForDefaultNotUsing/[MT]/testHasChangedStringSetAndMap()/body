{
  List list=queryForPropertyHasChanged(NotUsingModifiedFlagsEntity.class,entityId,"stringSet","stringMap");
  assertEquals(2,list.size());
  assertEquals(TestTools.makeList(1,8),extractRevisionNumbers(list));
}
