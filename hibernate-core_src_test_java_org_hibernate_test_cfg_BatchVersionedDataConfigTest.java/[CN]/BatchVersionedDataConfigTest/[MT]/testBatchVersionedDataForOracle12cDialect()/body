{
  Configuration cfg=new Configuration().configure(CFG_XML).setProperty(AvailableSettings.DIALECT,Oracle12cDialect.class.getName());
  SessionFactory sessionFactory=cfg.buildSessionFactory();
  assertThat(sessionFactory.getSessionFactoryOptions().isJdbcBatchVersionedData(),is(true));
}
