{
  Configuration cfg=new Configuration().configure(CFG_XML).setProperty(AvailableSettings.DIALECT,OracleDialect.class.getName());
  SessionFactory sessionFactory=cfg.buildSessionFactory();
  assertThat(sessionFactory.getSessionFactoryOptions().isJdbcBatchVersionedData(),is(false));
}
