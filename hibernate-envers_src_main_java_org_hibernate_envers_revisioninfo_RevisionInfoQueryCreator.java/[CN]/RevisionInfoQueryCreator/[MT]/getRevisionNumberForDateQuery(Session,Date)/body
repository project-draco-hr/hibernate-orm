{
  return session.createCriteria(revisionInfoEntityName).setProjection(Projections.max(revisionInfoIdName)).add(Restrictions.le(revisionInfoTimestampName,timestampAsDate ? date : date.getTime()));
}
