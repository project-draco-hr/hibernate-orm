{
  if (identifier == null) {
    return jdbcEnvironment.getCurrentCatalog() == null ? null : jdbcEnvironment.getCurrentCatalog().render(jdbcEnvironment.getDialect());
  }
  return toMetaDataText(identifier);
}
