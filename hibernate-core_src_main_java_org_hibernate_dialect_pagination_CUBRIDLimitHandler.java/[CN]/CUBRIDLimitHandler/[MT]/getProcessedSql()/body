{
  if (LimitHelper.useLimit(this,selection)) {
    boolean useLimitOffset=LimitHelper.hasFirstRow(selection);
    return new StringBuilder(sql.length() + 20).append(sql).append(useLimitOffset ? " limit ?, ?" : " limit ?").toString();
  }
 else {
    return sql;
  }
}
