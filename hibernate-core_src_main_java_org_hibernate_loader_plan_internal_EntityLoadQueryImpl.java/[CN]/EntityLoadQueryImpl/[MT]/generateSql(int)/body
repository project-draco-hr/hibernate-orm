{
  final EntityJoinWalker entityJoinWalker=new EntityJoinWalker(entityPersister,entityPersister.getIdentifierColumnNames(),batchSize,lockMode,sessionFactory,loadQueryInfluencers);
  return entityJoinWalker.getSQLString();
}
