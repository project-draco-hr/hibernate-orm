{
  return (matchTiming == GenerationTiming.INSERT && valueGeneration.getGenerationTiming().includesInsert()) || (matchTiming == GenerationTiming.ALWAYS && valueGeneration.getGenerationTiming().includesUpdate());
}
