{
  AuditConfiguration verCfg=CFGS.get(cfg);
  if (verCfg == null) {
    verCfg=new AuditConfiguration(cfg,classLoaderService);
    CFGS.put(cfg,verCfg);
    cfg.buildMappings();
  }
  return verCfg;
}
