{
  if (!LOG.isDebugEnabled() || !iter.hasNext())   return;
  LOG.listingEntities();
  int i=0;
  while (iter.hasNext()) {
    if (i++ > 20) {
      LOG.more();
      break;
    }
    LOG.debug(toString(iter.next(),entityMode));
  }
}
