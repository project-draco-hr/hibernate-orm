{
  EmbeddedCacheManager m;
  if (providedManager != null)   m=providedManager;
 else   m=super.createCacheManager(properties,serviceRegistry);
  if (afterCacheManagerCreated != null) {
    afterCacheManagerCreated.accept(this,m);
  }
  return m;
}
