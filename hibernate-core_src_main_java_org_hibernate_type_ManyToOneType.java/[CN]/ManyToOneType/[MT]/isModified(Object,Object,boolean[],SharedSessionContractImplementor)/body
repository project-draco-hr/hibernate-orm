{
  if (current == null) {
    return old != null;
  }
  if (old == null) {
    return true;
  }
  return getIdentifierOrUniqueKeyType(session.getFactory()).isDirty(old,getIdentifier(current,session),session);
}
