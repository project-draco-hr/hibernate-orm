{
  super(sessionFactory,lockMode,sessionFactory.getCollectionPersister(ownerEntityName + '.' + ownerProperty),new PropertyPath(),null);
  this.ownerEntityName=ownerEntityName;
  this.ownerProperty=ownerProperty;
}
