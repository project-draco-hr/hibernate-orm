{
  super(cache,name,null,transactionManager);
  localOnly=CacheHelper.isClusteredInvalidation(cache);
  if (!localOnly) {
    localOnly=CacheHelper.isClusteredReplication(cache) && PropertiesHelper.getBoolean(QUERY_CACHE_LOCAL_ONLY_PROP,properties,false);
  }
}
