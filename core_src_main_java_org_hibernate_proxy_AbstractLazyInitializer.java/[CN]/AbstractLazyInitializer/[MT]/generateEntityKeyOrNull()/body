{
  if (getIdentifier() == null || session == null || entityName == null) {
    return null;
  }
  return new EntityKey(getIdentifier(),session.getFactory().getEntityPersister(entityName),session.getEntityMode());
}
