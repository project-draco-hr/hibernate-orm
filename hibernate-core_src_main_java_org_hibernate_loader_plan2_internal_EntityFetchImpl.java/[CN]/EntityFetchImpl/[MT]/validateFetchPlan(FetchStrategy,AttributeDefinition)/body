{
  if (attributeDefinition.getType().isCollectionType()) {
    throw new WalkingException("Encountered collection attribute in identifier fetches: " + attributeDefinition.getSource() + "."+ attributeDefinition.getName());
  }
}
