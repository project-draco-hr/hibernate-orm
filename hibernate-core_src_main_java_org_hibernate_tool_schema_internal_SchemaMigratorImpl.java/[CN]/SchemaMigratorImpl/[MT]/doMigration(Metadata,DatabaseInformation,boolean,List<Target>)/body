{
  for (  Target target : targets) {
    target.prepare();
  }
  doMigrationToTargets(metadata,existingDatabase,createSchemas,targets);
  for (  Target target : targets) {
    target.release();
  }
}
