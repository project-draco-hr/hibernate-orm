{
  for (  Target target : targets) {
    target.prepare();
  }
  doMigrationToTargets(metadata,existingDatabase,createNamespaces,targets);
  for (  Target target : targets) {
    target.release();
  }
}
