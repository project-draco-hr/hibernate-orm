{
  StringBuilder buffer=new StringBuilder().append("function('").append(getFunctionName()).append("', ");
  renderArguments(buffer,renderingContext);
  buffer.append(')');
  return buffer.toString();
}
