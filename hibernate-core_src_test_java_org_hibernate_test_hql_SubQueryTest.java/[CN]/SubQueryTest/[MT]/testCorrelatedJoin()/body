{
  Session s=openSession();
  s.beginTransaction();
  final String qry="from Root as r " + "where r.branch.branchName = 'some branch name' " + "  and exists( from r.branch.leaves as s where s.leafName = 'some leaf name')";
  s.createQuery(qry).list();
  s.getTransaction().commit();
  s.close();
}
