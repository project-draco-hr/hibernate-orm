{
  Assert.assertEquals(House_.class.getSuperclass(),Building_.class,"Entity with mapped superclass should inherit at the metamodel level");
  Assert.assertEquals(Building_.class.getSuperclass(),Area_.class,"mapped superclass with mapped superclass should inherit at the metamodel level");
}
