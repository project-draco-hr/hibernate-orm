{
  try {
    PreparedStatement ps=doPrepare();
    postProcess(ps);
    return ps;
  }
 catch (  SQLException sqle) {
    LOG.sqlExceptionEscapedProxy(sqle);
    throw sqlExceptionHelper.convert(sqle,"could not prepare statement",sql);
  }
}
