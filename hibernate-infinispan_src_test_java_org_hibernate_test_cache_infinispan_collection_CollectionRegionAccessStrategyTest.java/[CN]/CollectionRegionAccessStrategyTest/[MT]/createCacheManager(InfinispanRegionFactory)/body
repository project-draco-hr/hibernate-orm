{
  EmbeddedCacheManager cacheManager=TestCacheManagerFactory.createCacheManager(false);
  cacheManager.defineConfiguration(InfinispanRegionFactory.PENDING_PUTS_CACHE_NAME,regionFactory.getPendingPutsCacheConfiguration());
  return cacheManager;
}
