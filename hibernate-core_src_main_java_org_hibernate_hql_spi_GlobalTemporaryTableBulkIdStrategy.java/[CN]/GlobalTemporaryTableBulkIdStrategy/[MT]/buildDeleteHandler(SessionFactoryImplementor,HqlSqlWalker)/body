{
  return new TableBasedDeleteHandlerImpl(factory,walker){
    @Override protected void releaseFromUse(    Queryable persister,    SessionImplementor session){
      cleanUpRows(determineIdTableName(persister),session);
    }
  }
;
}
