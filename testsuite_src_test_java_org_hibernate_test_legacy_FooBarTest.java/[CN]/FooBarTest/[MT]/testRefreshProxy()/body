{
  Session s=openSession();
  Glarch g=new Glarch();
  Serializable gid=s.save(g);
  s.flush();
  s.clear();
  GlarchProxy gp=(GlarchProxy)s.load(Glarch.class,gid);
  gp.getName();
  s.refresh(gp);
  s.delete(gp);
  s.flush();
  s.connection().commit();
  s.close();
}
