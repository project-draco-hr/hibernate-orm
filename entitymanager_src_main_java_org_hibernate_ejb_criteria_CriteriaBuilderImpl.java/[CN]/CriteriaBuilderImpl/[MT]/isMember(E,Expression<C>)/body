{
  if (!CollectionExpression.class.isInstance(collectionExpression)) {
    throw new IllegalArgumentException("unknown collection expression type [" + collectionExpression.getClass().getName() + "]");
  }
  return new MemberOfPredicate<E,C>(this,e,(CollectionExpression<C>)collectionExpression);
}
