{
  int numberOfRowCounts=rowCounts.length;
  if (numberOfRowCounts != batchSize)   LOG.jdbcDriverDidNotReturnExpectedNumberOfRowCounts();
  for (int i=0; i < numberOfRowCounts; i++) {
    expectations[i].verifyOutcome(rowCounts[i],ps,i);
  }
}
