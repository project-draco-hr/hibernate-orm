{
  EmbeddedCacheManager cacheManager=TestCacheManagerFactory.createCacheManager(false);
  cacheManager.defineConfiguration(InfinispanRegionFactory.PENDING_PUTS_CACHE_NAME,InfinispanRegionFactory.DEFAULT_PENDING_PUTS_CACHE_CONFIGURATION);
  return cacheManager;
}
