{
  EntityBinding entityBinding=new EntityBinding();
  entityBinding.setInheritanceType(InheritanceType.TABLE_PER_CLASS);
  entityBinding.setSuperEntityBinding(bindingContext.getMetadataImplementor().getEntityBinding(superType.getName()));
  doBasicEntityBinding(entityBinding);
  return entityBinding;
}
