{
  if (isAllEmpty) {
    return true;
  }
 else   if (includedClassNames.contains(className)) {
    return true;
  }
 else   if (isInIncludedPackage(className)) {
    return true;
  }
 else {
    return false;
  }
}
