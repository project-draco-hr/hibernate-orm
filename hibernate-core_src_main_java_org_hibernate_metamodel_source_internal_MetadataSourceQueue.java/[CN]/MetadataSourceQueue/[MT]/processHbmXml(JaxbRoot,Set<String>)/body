{
  try {
    metadata.getHibernateXmlBinder().bindRoot(jaxbRoot,entityNames);
  }
 catch (  MappingException me) {
    throw new InvalidMappingException(jaxbRoot.getOrigin().getType().toString(),jaxbRoot.getOrigin().getName(),me);
  }
}
