{
  if (other == null) {
    return false;
  }
  if (this == other) {
    return true;
  }
  if (hashCode != other.hashCode() || !(other instanceof CacheKey)) {
    return false;
  }
  final CacheKey that=(CacheKey)other;
  return EqualsHelper.equals(getTenantId(),that.getTenantId()) && type.isEqual(key,that.key);
}
