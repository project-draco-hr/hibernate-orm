{
  StringBuilder caseExpr=new StringBuilder();
  caseExpr.append("case ").append(((ExpressionImplementor)getExpression()).render(renderingContext)).append(' ');
  for (  WhenClause whenClause : getWhenClauses()) {
    caseExpr.append(((ExpressionImplementor)whenClause.getCondition()).render(renderingContext)).append(" then ").append(((ExpressionImplementor)whenClause.getResult()).render(renderingContext));
  }
  caseExpr.append(" else ").append(((ExpressionImplementor)getOtherwiseResult()).render(renderingContext)).append(" end");
  return caseExpr.toString();
}
