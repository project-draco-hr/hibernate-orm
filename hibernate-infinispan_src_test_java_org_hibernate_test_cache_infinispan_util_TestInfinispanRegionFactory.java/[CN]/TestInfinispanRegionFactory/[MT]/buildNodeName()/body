{
  StringBuilder sb=new StringBuilder("Node");
  int id=counter.getAndIncrement();
  int alphabet='Z' - 'A';
  do {
    sb.append((char)(id % alphabet + 'A'));
    id/=alphabet;
  }
 while (id > alphabet);
  return sb.toString();
}
