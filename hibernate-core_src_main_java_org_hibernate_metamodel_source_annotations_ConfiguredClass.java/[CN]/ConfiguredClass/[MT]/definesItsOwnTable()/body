{
  if (isMappedSuperClass() || isEmbeddable()) {
    return false;
  }
  if (InheritanceType.SINGLE_TABLE.equals(inheritanceType)) {
    if (isRoot()) {
      return true;
    }
 else {
      return false;
    }
  }
  return true;
}
