{
  ParameterMetadata value=parameterMetadataCache.get(query);
  if (value == null) {
    value=factory.getServiceRegistry().getService(ParameterMetadataRecognizer.class).getParameterMetadata(query);
    parameterMetadataCache.putIfAbsent(query,value);
  }
  return value;
}
