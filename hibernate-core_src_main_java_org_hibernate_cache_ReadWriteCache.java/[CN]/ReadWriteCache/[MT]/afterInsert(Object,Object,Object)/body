{
  LOG.inserting(key);
  try {
    cache.lock(key);
    Lockable lockable=(Lockable)cache.get(key);
    if (lockable == null) {
      cache.update(key,new Item(value,version,cache.nextTimestamp()));
      LOG.inserted(key);
      return true;
    }
 else {
      return false;
    }
  }
  finally {
    cache.unlock(key);
  }
}
