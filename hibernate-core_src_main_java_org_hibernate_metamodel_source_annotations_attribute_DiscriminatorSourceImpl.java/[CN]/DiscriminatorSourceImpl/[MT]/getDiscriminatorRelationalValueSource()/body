{
  return entityClass.getDiscriminatorFormula() != null ? new DerivedValueSourceImpl(entityClass.getDiscriminatorFormula()) : new ColumnValuesSourceImpl(entityClass.getDiscriminatorColumnValues());
}
