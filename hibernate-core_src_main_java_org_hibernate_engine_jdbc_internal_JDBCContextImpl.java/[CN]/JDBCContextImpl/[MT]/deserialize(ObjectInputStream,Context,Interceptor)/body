{
  JDBCContextImpl jdbcContext=new JDBCContextImpl();
  jdbcContext.owner=context;
  jdbcContext.connectionManager=ConnectionManagerImpl.deserialize(ois,context.getFactory(),interceptor,context.getConnectionReleaseMode(),jdbcContext);
  return jdbcContext;
}
