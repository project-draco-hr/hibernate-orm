{
  super();
  registerColumnType(MySQLGeometryTypeDescriptor.INSTANCE.getSqlType(),MySQLGeometryTypeDescriptor.INSTANCE.getTypeName());
  for (  Map.Entry<String,StandardSQLFunction> entry : new MySQLSpatialFunctions()) {
    registerFunction(entry.getKey(),entry.getValue());
  }
}
