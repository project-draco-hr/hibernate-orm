{
  String path=pathToTraversableObject.equals("") ? traversableProperty : pathToTraversableObject + "." + traversableProperty;
  String[] paths=path.split("\\[.*\\]");
  path="";
  for (  String subpath : paths) {
    path+=subpath;
  }
  return path;
}
