{
  int loc=propertyPath.lastIndexOf(".");
  final String unqualifiedPropertyName=loc < 0 ? propertyPath : propertyPath.substring(loc + 1);
  return ownerEntityTable + '_' + unqualifiedPropertyName;
}
