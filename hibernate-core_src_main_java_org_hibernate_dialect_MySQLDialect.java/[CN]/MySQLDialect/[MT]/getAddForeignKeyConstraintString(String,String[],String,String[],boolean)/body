{
  final String cols=StringHelper.join(", ",foreignKey);
  final String referencedCols=StringHelper.join(", ",primaryKey);
  return String.format(" add index %s (%s), add constraint %s foreign key (%s) references %s (%s)",constraintName,cols,constraintName,cols,referencedTable,referencedCols);
}
