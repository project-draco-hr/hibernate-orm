{
  checkTransactionSynchStatus();
  return !isClosed() && getTransactionCoordinator().getTransactionDriverControl().getStatus() == TransactionStatus.ACTIVE && getTransactionCoordinator().isJoined();
}
