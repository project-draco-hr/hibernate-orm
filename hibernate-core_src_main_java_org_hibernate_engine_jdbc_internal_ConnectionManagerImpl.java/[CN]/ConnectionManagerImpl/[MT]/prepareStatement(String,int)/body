{
  if (autoGeneratedKeys == PreparedStatement.RETURN_GENERATED_KEYS) {
    checkAutoGeneratedKeysSupportEnabled();
  }
  executeBatch();
  return proxiedConnection.prepareStatement(getSQL(sql),autoGeneratedKeys);
}
