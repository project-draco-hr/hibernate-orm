{
  sql=getSQL(sql);
  PreparedStatement result=(isCallable ? proxiedConnection.prepareCall(sql) : proxiedConnection.prepareStatement(sql));
  setStatementFetchSize(result);
  logicalConnection.getResourceRegistry().registerLastQuery(result);
  return result;
}
