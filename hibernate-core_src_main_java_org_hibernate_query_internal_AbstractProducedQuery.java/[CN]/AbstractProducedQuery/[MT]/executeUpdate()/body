{
  queryParameterBindings.verifyParametersBound(false);
  beforeQuery();
  try {
    return getProducer().executeUpdate(queryParameterBindings.expandListValuedParameters(getQueryString(),getProducer()),getQueryParameters());
  }
  finally {
    afterQuery();
  }
}
