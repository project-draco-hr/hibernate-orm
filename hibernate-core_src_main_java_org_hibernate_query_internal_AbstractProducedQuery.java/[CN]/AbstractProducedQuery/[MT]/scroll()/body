{
  queryParameterBindings.verifyParametersBound(false);
  beforeQuery();
  try {
    return getProducer().scroll(queryParameterBindings.expandListValuedParameters(getQueryString(),getProducer()),getQueryParameters());
  }
  finally {
    afterQuery();
  }
}
