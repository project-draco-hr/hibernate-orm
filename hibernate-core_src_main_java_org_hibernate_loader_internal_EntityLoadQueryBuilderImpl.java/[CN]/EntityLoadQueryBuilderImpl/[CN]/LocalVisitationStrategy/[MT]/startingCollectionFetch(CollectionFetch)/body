{
  JoinableAssociationImpl assoc=new JoinableAssociationImpl(collectionFetch,getCurrentEntityReference(),"",false,sessionFactory,loadQueryInfluencers.getEnabledFilters());
  associations.add(assoc);
  pushToStack(collectionReferenceStack,collectionFetch);
}
