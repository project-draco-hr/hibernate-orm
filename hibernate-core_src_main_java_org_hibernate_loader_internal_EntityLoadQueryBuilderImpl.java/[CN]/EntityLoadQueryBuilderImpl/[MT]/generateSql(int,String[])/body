{
  final EntityLoadQueryImpl loadQuery=new EntityLoadQueryImpl(sessionFactory,getRootEntityReturn(),associations,suffixes);
  return loadQuery.generateSql(uniqueKey,batchSize,getRootEntityReturn().getLockMode());
}
