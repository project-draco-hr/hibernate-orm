{
  if (!region.checkValid())   return null;
  region.ensureRegionRootExists();
  Object val=CacheHelper.get(cache,regionFqn,key);
  if (val == null) {
    putValidator.registerPendingPut(key);
  }
  return val;
}
