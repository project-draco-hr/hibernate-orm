{
  Type identifierType=persister.getOwnerEntityPersister().getIdentifierType();
  if (id.getClass().isAssignableFrom(identifierType.getReturnedClass())) {
    s.append(identifierType.toLoggableString(id,factory));
  }
}
