{
  if (connection != null) {
    return;
  }
  log.debug("opening JDBC connection");
  try {
    connection=factory.getConnectionProvider().getConnection();
  }
 catch (  SQLException sqle) {
    throw factory.getSQLExceptionHelper().convert(sqle,"Cannot open connection");
  }
  callback.physicalConnectionObtained(connection);
}
