{
  final Object configuredOption=(options == null) ? null : options.get(AvailableSettings.SCHEMA_FILTER_PROVIDER);
  return serviceRegistry.getService(StrategySelector.class).resolveDefaultableStrategy(SchemaFilterProvider.class,configuredOption,DefaultSchemaFilterProvider.INSTANCE);
}
