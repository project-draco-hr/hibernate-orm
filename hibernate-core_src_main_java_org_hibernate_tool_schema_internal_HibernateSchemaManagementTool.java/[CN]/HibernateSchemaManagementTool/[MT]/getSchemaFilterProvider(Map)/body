{
  return serviceRegistry.getService(StrategySelector.class).resolveDefaultableStrategy(SchemaFilterProvider.class,options.get(AvailableSettings.SCHEMA_FILTER_PROVIDER),DefaultSchemaFilterProvider.INSTANCE);
}
