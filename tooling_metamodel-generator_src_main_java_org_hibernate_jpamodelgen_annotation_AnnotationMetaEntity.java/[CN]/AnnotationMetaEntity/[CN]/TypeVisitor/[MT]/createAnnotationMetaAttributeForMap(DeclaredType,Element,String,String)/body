{
  String keyType;
  if (TypeUtils.containsAnnotation(element,MapKeyClass.class)) {
    TypeMirror typeMirror=(TypeMirror)TypeUtils.getAnnotationValue(TypeUtils.getAnnotationMirror(element,MapKeyClass.class),DEFAULT_ANNOTATION_PARAMETER_NAME);
    keyType=typeMirror.toString();
  }
 else {
    keyType=getKeyType(declaredType);
  }
  return new AnnotationMetaMap(parent,element,collection,keyType,getElementType(declaredType,targetEntity));
}
