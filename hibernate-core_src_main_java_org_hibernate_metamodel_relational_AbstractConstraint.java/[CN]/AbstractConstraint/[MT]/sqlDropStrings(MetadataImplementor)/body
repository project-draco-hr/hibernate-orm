{
  Dialect dialect=getDialect(metadata);
  if (isCreationVetoed(dialect)) {
    return null;
  }
 else {
    return new String[]{new StringBuffer().append("alter table ").append(getTable().getQualifiedName(dialect)).append(" drop constraint ").append(dialect.quote(getName())).toString()};
  }
}
