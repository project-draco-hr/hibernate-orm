{
  Type type=propertyValue.getType();
  if ((type instanceof BagType || type instanceof SetType || type instanceof MapType) && (propertyValue.getElement() instanceof OneToMany) && (propertyValue.isInverse())) {
    addOneToManyAttached();
  }
 else {
    addWithMiddleTable();
  }
}
