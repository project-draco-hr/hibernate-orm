{
  Type type=propertyValue.getType();
  if ((type instanceof BagType || type instanceof SetType || type instanceof MapType|| type instanceof ListType) && (propertyValue.getElement() instanceof OneToMany) && (propertyValue.isInverse())) {
    addOneToManyAttached();
  }
 else {
    addWithMiddleTable();
  }
}
