{
  boolean result=hasOrders;
  if (!result) {
    for (    final AuditAssociationQueryImplementor<?> sub : associationQueries) {
      if (sub.hasOrders()) {
        result=true;
        break;
      }
    }
  }
  return result;
}
