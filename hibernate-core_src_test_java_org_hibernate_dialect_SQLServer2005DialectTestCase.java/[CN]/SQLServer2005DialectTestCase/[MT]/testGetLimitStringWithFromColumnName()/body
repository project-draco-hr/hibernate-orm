{
  final String fromColumnNameSQL="select persistent0_.rid as rid1688_, " + "persistent0_.deviationfromtarget as deviati16_1688_, " + "persistent0_.sortindex as sortindex1688_ "+ "from m_evalstate persistent0_ "+ "where persistent0_.customerid=?";
  assertEquals("WITH query AS (select persistent0_.rid as rid1688_, " + "persistent0_.deviationfromtarget as deviati16_1688_, " + "persistent0_.sortindex as sortindex1688_, "+ "ROW_NUMBER() OVER (ORDER BY CURRENT_TIMESTAMP) as __hibernate_row_nr__ "+ "from m_evalstate persistent0_ "+ "where persistent0_.customerid=?) "+ "SELECT * FROM query WHERE __hibernate_row_nr__ >= ? AND __hibernate_row_nr__ < ?",dialect.getLimitString(fromColumnNameSQL,1,10));
}
