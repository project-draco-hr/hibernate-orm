{
  if (persister.implementsLifecycle(session.getEntityMode())) {
    LOG.callingOnDelete();
    if (((Lifecycle)entity).onDelete(session)) {
      LOG.deletionVetoed();
      return true;
    }
  }
  return false;
}
