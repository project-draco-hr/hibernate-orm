{
  LOG.debugf("Cache lookup : key[%s]",key);
  if (key == null) {
    return null;
  }
  Object result=cache.get(key);
  if (result != null) {
    LOG.debugf("Cache hit: %s",key);
  }
  return result;
}
