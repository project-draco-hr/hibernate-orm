{
  log.debug("Adding JDBC dependency[{}] resolved from matrix.gradle",matrixNode.jdbcDependencies.getName());
  project.getDependencies().add(matrixNode.jdbcDependencies.getName(),dependencyNotation);
}
