{
  if (puts.size() > 0) {
    initialize(true);
    Iterator itr=puts.entrySet().iterator();
    while (itr.hasNext()) {
      Map.Entry entry=(Entry)itr.next();
      put(entry.getKey(),entry.getValue());
    }
  }
}
