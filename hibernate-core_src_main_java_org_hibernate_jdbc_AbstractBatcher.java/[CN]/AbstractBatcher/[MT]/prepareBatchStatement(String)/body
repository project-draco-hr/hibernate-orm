{
  sql=getSQL(sql);
  if (!sql.equals(batchUpdateSQL)) {
    batchUpdate=prepareStatement(sql);
    batchUpdateSQL=sql;
  }
 else {
    LOG.reusingPreparedStatement();
    log(sql);
  }
  return batchUpdate;
}
