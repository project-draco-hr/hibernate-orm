{
  for (  String command : commands) {
    try {
      jdbcStatement().execute(command);
    }
 catch (    SQLException e) {
      throw new PersistenceException("Unable to execute JPA schema generation drop command [" + command + "]");
    }
  }
}
