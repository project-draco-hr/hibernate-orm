{
  for (  Object element : value) {
    if (!isValidBindValue(getParameterType(),element,temporalType)) {
      throw new IllegalArgumentException(String.format("Parameter value element [%s] did not match expected type [%s (%s)]",element,getParameterType().getName(),extractName(temporalType)));
    }
  }
}
