{
  super(jbcCache,regionName,regionPrefix,null);
  localOnly=CacheHelper.isClusteredInvalidation(jbcCache);
  if (!localOnly) {
    localOnly=CacheHelper.isClusteredReplication(jbcCache) && PropertiesHelper.getBoolean(QUERY_CACHE_LOCAL_ONLY_PROP,properties,false);
  }
}
