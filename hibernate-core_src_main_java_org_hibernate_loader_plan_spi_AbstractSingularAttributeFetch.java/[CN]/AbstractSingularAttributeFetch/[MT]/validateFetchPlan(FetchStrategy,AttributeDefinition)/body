{
  if (fetchStrategy.getStyle() == FetchStyle.JOIN) {
    if (this.fetchStrategy.getStyle() != FetchStyle.JOIN) {
      throw new HibernateException(String.format("Cannot specify join fetch from owner [%s] that is a non-joined fetch : %s",getPropertyPath().getFullPath(),attributeDefinition.getName()));
    }
  }
}
