{
  Element class_mapping=createEntityCommon(document,"subclass",entityName,tableName,schema,catalog,discriminatorValue);
  class_mapping.addAttribute("extends",extendsEntityName);
  return class_mapping;
}
