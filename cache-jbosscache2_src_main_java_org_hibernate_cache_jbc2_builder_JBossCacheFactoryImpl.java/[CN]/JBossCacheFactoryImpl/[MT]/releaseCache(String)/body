{
synchronized (caches) {
    if (!caches.containsKey(configName))     throw new IllegalStateException(configName + " not registered");
    if (decrementCheckout(configName) == 0) {
      Cache cache=(Cache)caches.remove(configName);
      destroyCache(cache);
    }
  }
}
