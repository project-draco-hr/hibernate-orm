{
  Parameter param=getParameter(name);
  if (param.getJavaType() != null) {
    throw new IllegalArgumentException("Parameter type [" + param.getJavaType().getName() + "] is not assignment compatible with requested type ["+ type.getName()+ "]");
  }
  return param;
}
