{
  int joinType=super.getJoinType(persister,path,propertyNumber,associationType,metadataFetchMode,metadataCascadeStyle,lhsTable,lhsColumns,nullable,currentDepth);
  if (joinType == JoinFragment.LEFT_OUTER_JOIN && path.isRoot()) {
    joinType=JoinFragment.INNER_JOIN;
  }
  return joinType;
}
