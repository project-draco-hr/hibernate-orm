{
  out.defaultWriteObject();
  if (versionComparator instanceof ComparableComparator)   out.writeByte(0);
 else   if (versionComparator instanceof CalendarComparator)   out.writeByte(1);
 else {
    out.writeByte(999);
    out.writeObject(versionComparator);
  }
}
