{
  String fileContent=new String(Files.readAllBytes(output.toPath()));
  assertThat("Script file : " + fileContent.toLowerCase(),fileContent.toLowerCase().contains("create table test_entity"),is(true));
}
