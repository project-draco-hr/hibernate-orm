{
  final FetchSource registeredFetchSource=loadPlanBuildingContext.registeredFetchSource(attributeDefinition.getAssociationKey());
  if (isKeyManyToOneBidirectionalAttributeDefinition(attributeDefinition,loadPlanBuildingContext)) {
    return new KeyManyToOneBidirectionalEntityFetchImpl(this,attributeDefinition,fetchStrategy,fetchedJoin,registeredFetchSource.resolveEntityReference());
  }
 else {
    return super.createEntityFetch(attributeDefinition,fetchStrategy,fetchedJoin,loadPlanBuildingContext);
  }
}
