{
  dialectFactory=new DialectFactoryImpl();
  dialectFactory.setStrategySelector(new StrategySelectorBuilder().buildSelector(new ClassLoaderServiceImpl(getClass().getClassLoader())));
  dialectFactory.setDialectResolver(new StandardDialectResolver());
}
