{
  if (this == o) {
    return true;
  }
  if (o == null || getClass() != o.getClass()) {
    return false;
  }
  AbstractConstraintSource that=(AbstractConstraintSource)o;
  return EqualsHelper.equals(name,that.name) && EqualsHelper.equals(tableName,that.tableName) && columnNames.equals(that.columnNames);
}
