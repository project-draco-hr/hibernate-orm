{
  long unversioned=measureTime(new Runnable(){
    public void run(){
      insertUnversioned();
    }
  }
);
  long versioned=measureTime(new Runnable(){
    public void run(){
      insertVersioned();
    }
  }
);
  return Pair.make(unversioned,versioned);
}
