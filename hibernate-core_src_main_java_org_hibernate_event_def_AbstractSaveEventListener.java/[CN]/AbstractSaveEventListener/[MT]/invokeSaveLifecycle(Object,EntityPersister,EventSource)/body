{
  if (persister.implementsLifecycle(source.getEntityMode())) {
    LOG.callingOnSave();
    if (((Lifecycle)entity).onSave(source)) {
      LOG.insertionVetoed();
      return true;
    }
  }
  return false;
}
