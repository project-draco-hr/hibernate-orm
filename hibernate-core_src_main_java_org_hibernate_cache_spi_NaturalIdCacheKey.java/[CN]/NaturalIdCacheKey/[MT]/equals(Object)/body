{
  if (this == obj)   return true;
  if (obj == null)   return false;
  if (getClass() != obj.getClass())   return false;
  NaturalIdCacheKey other=(NaturalIdCacheKey)obj;
  if (this.hashCode != other.hashCode) {
    return false;
  }
  if (entityName == null) {
    if (other.entityName != null)     return false;
  }
 else   if (!entityName.equals(other.entityName))   return false;
  if (!Arrays.equals(naturalId,other.naturalId))   return false;
  if (tenantId == null) {
    if (other.tenantId != null)     return false;
  }
 else   if (!tenantId.equals(other.tenantId))   return false;
  for (int i=0; i < naturalId.length; i++) {
    if (!types[i].isEqual(naturalId[i],other.naturalId[i])) {
      return false;
    }
  }
  return true;
}
