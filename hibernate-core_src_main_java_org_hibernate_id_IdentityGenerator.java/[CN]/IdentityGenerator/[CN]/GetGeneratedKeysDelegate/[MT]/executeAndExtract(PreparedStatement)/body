{
  insert.executeUpdate();
  ResultSet rs=null;
  try {
    rs=insert.getGeneratedKeys();
    return IdentifierGeneratorHelper.getGeneratedIdentity(rs,persister.getIdentifierColumnNames()[0],persister.getIdentifierType());
  }
  finally {
    if (rs != null) {
      rs.close();
    }
  }
}
