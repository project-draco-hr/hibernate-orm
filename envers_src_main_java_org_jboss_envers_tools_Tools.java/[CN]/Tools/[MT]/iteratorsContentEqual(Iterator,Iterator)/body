{
  while (iter1.hasNext() && iter2.hasNext()) {
    if (!iter1.next().equals(iter2.next())) {
      return false;
    }
  }
  if (iter1.hasNext() || iter2.hasNext()) {
    return false;
  }
  return true;
}
