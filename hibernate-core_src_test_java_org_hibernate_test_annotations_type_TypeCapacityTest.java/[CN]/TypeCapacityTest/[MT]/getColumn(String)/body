{
  Iterator iter=configuration().createMappings().getTable(null,null,"TypeCapacityTest$TypeCapacityEntity").getColumnIterator();
  while (iter.hasNext()) {
    Column c=(Column)iter.next();
    if (c.getName().equals(name)) {
      return c;
    }
  }
  return null;
}
