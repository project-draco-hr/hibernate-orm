{
  if (connectionHandlingMode.getReleaseMode() == ConnectionReleaseMode.AFTER_STATEMENT) {
    if (!jdbcConnectionAccess.supportsAggressiveRelease()) {
      throw new HibernateException("Connection provider reports to not support aggressive release, but ATER_STATEMENT releasing was requested");
    }
  }
}
