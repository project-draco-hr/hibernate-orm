{
  ssr=new StandardServiceRegistryBuilder().applySetting("javax.persistence.validation.mode","none").build();
  metadata=new MetadataSources(ssr).addResource("org/hibernate/test/legacy/Wicked.hbm.xml").buildMetadata();
}
