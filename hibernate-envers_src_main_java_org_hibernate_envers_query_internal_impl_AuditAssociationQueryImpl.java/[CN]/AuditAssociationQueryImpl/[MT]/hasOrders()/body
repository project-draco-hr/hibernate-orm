{
  boolean result=hasOrders;
  if (!result) {
    for (    final AuditAssociationQueryImpl<?> sub : associationQueries) {
      if (sub.hasOrders()) {
        result=true;
        break;
      }
    }
  }
  return result;
}
