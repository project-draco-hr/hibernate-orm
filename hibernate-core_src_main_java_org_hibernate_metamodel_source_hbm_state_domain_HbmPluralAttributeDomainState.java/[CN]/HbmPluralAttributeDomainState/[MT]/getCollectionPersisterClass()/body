{
  try {
    return DomHelper.extractClassAttributeValue(getElement(),"persister");
  }
 catch (  ClassNotFoundException cnfe) {
    throw new MappingException("Could not find collection persister class: " + getElement().attributeValue("persister"));
  }
}
