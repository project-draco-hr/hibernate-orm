{
  if (fromElement.getFromClause() == getWalker().getCurrentFromClause() && areSame(classAlias,fromElement.getClassAlias())) {
    return true;
  }
  return getWalker().getCurrentClauseType() != SqlTokenTypes.FROM;
}
