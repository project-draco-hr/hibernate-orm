{
  if (!(object instanceof Prcontra)) {
    return false;
  }
  Prcontra other=(Prcontra)object;
  if (this.prcontraPK != other.prcontraPK && (this.prcontraPK == null || !this.prcontraPK.equals(other.prcontraPK))) {
    return false;
  }
  return true;
}
