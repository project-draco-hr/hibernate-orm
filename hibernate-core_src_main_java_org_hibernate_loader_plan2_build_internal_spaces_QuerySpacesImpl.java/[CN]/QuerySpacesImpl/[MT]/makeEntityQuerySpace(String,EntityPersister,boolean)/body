{
  checkQuerySpaceDoesNotExist(uid);
  final EntityQuerySpaceImpl space=new EntityQuerySpaceImpl(entityPersister,uid,this,canJoinsBeRequired,sessionFactory);
  registerQuerySpace(space);
  return space;
}
