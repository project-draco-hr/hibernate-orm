{
  checkQuerySpaceDoesNotExist(uid);
  final ExpandingCompositeQuerySpace space=new CompositeQuerySpaceImpl(compositePropertyMapping,uid,this,canJoinsBeRequired,sessionFactory);
  registerQuerySpace(space);
  return space;
}
