{
  pe.getMessager().printMessage(Kind.NOTE,"Scanning " + membersOfClass.size() + " "+ membersKind+ " for "+ element.toString());
  AccessType explicitAccessType;
  if (elementAccessType == membersKind) {
    explicitAccessType=null;
  }
 else {
    explicitAccessType=membersKind;
  }
  for (  Element memberOfClass : membersOfClass) {
    MetaAttribute result=memberOfClass.asType().accept(new TypeVisitor(this,explicitAccessType),memberOfClass);
    if (result != null) {
      membersFound.add(result);
    }
  }
}
