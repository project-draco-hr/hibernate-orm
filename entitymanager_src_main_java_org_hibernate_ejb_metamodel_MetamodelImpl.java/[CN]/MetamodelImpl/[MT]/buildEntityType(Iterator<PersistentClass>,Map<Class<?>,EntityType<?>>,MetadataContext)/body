{
  PersistentClass persistentClass=classes.next();
  @SuppressWarnings("unchecked") final Class<X> clazz=persistentClass.getMappedClass();
  if (clazz != null) {
    localEntities.put(clazz,new EntityTypeImpl<X>(clazz,persistentClass,context));
  }
}
