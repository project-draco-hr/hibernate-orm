{
  Session s=openSession();
  s.createQuery("from MyEntity e join treat(e.other as MySubclassEntity) o").list();
  s.createQuery("from MyEntity e join TREAT(e.other as MySubclassEntity) o").list();
  s.close();
}
