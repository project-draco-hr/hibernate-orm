{
  try {
    Option option=new Option();
    option.setCacheModeLocal(true);
    option.setFailSilently(true);
    jbcNode.jbcTreeCache.remove(regionFqn,option);
  }
 catch (  Exception e) {
    throw new CacheException(e);
  }
}
