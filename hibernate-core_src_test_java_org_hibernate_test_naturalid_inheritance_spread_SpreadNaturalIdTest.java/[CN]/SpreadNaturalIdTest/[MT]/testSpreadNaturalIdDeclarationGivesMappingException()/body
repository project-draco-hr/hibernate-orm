{
  Configuration cfg=new Configuration().addAnnotatedClass(Principal.class).addAnnotatedClass(User.class);
  try {
    cfg.buildMappings();
    fail("Expected binders to throw an exception");
  }
 catch (  AnnotationException expected) {
  }
}
