{
  try {
    new MetadataSources().addAnnotatedClass(Principal.class).addAnnotatedClass(User.class).buildMetadata();
    fail("Expected binders to throw an exception");
  }
 catch (  AnnotationException expected) {
  }
}
