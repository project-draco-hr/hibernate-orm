{
  if (this.hash == 0) {
    final int newHash=System.identityHashCode(key);
    if (newHash == 0) {
      this.hash=-1;
      return -1;
    }
 else {
      this.hash=newHash;
      return newHash;
    }
  }
  return hash;
}
