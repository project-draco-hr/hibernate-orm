{
  Element owner=(Element)target;
  if (!super.propertyType.isXMLElement()) {
    if (value == null) {
      owner.setText(null);
    }
 else {
      super.propertyType.setToXMLNode(owner,value,factory);
    }
  }
}
