{
  Integer timeout=getInteger(hints,QueryHints.TIMEOUT_JPA,query);
  if (timeout == null)   return getInteger(hints,QueryHints.TIMEOUT_HIBERNATE,query);
  return new Integer((int)Math.round(timeout.doubleValue() / 1000.0));
}
