{
  final SimpleSingularAttributeBinding binding=new SimpleSingularAttributeBinding(this,attribute,isNullable(),isAlternateUniqueKey());
  registerAttributeBinding(attribute.getName(),binding);
  return binding;
}
