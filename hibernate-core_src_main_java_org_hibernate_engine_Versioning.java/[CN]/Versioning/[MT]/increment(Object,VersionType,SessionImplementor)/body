{
  Object next=versionType.next(version,session);
  if (LOG.isTraceEnabled())   LOG.incrementing(versionType.toLoggableString(version,session.getFactory()),versionType.toLoggableString(next,session.getFactory()));
  return next;
}
