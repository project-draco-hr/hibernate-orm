{
  if (globalAnnotations.hasGlobalConfiguration()) {
    indexBuilder.collectGlobalConfigurationFromIndex(globalAnnotations);
    new GlobalConfigurationMocker(indexBuilder,globalAnnotations).parser();
  }
}
